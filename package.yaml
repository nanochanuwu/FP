name: FP
version: 0.1.0.0
synopsis: My Haskell project
description: See FP.pdf
maintainer: My Name <my.email@example.com>
category: Logic

ghc-options: -Wall

dependencies:
  - base
  - random
  - QuickCheck

library:
  source-dirs: lib

executables:
  myprogram:
    main: Main.lhs
    source-dirs: exec
    dependencies:
      - FP

tests:
  tests:
    main: tests.lhs
    source-dirs: test
    dependencies:
      - FP
      - QuickCheck
      - hspec
